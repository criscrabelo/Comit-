node_modules/
db.json
*.backup
URL_PUBLICA.txt
.env

# Backend
dist/
server/dist/
*.tsbuildinfo

# Backups. Nunca no repositorio: um arquivo destes e a base inteira, com CPF,
# contrato e situacao juridica de clientes reais. O destino oficial fica fora
# da arvore do projeto (BACKUP_DIRETORIO), mas um backup gerado por engano no
# diretorio de trabalho nao pode escapar num `git add -A`.
*.dump
*.dump.enc
*.sql.gz
backups/
